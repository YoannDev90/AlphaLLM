{
  "type": "function",
  "function": {
    "name": "generate_image",
    "description": "Generate an image based on a text prompt",
    "parameters": {
      "type": "object",
      "properties": {
        "prompt": {
          "type": "string",
          "description": "The text prompt describing the image to generate"
        },
        "prompt_enhance": {
          "type": "boolean",
          "description": "Whether to enhance the text prompt before generating the image (default: true )"
        },
        "model": {
          "type": "string",
          "description": "The image generation model to use (optional, defaults to auto)",
          "enum": ["flux", "gptimage", "zimage"]
        },
        "style": {
          "type": "string",
          "description": "The artistic style to apply to the image (optional)",
          "enum": ["3d_render", "abstract", "animated", "art_deco", "comic_book", "concept_art", "drawing", "fantasy", "film_noir", "gothic", "graffiti", "illustration", "kawaii", "landscape", "logo", "minimalist", "oil_painting", "pixel_art", "pop_art", "portrait", "sci_fi", "steampunk", "surreal", "vintage_retro", "watercolor"]
        }
      },
      "required": ["prompt"]
    }
  }
}
