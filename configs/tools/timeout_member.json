{
  "name": "timeout_member",
  "description": "Timeout (mute) a member for a specified duration to prevent them from speaking",
  "parameters": {
    "type": "object",
    "properties": {
      "user_id": {
        "type": "string",
        "description": "The Snowflake ID of the user to timeout (18-digit ID)"
      },
      "duration_minutes": {
        "type": "integer",
        "description": "The duration of the timeout in minutes",
        "default": 10
      },
      "reason": {
        "type": "string",
        "description": "Optional: The reason for the timeout"
      }
    },
    "required": ["user_id", "duration_minutes"]
  }
}
