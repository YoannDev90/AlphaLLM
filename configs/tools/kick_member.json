{
  "name": "kick_member",
  "description": "Kick a member from a Discord server",
  "parameters": {
    "type": "object",
    "properties": {
      "user_id": {
        "type": "string",
        "description": "The Snowflake ID of the user to kick (the 18-digit ID)"
      },
      "reason": {
        "type": "string",
        "description": "Optional: The reason for kicking the member. Leave empty if no reason is specified."
      }
    },
    "required": ["user_id"]
  }
}
