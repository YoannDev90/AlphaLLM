{
  "name": "remove_role",
  "description": "Remove an existing role from a member in the current Discord server",
  "parameters": {
    "type": "object",
    "properties": {
      "user_id": {
        "type": "string",
        "description": "The Snowflake ID of the user (18-digit ID)"
      },
      "role_id": {
        "type": "string",
        "description": "The Snowflake ID of the role to remove"
      }
    },
    "required": ["user_id", "role_id"]
  }
}
