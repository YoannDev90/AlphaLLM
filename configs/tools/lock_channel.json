{
  "name": "lock_channel",
  "description": "Lock or unlock a text channel by modifying @everyone's send_messages permission",
  "parameters": {
    "type": "object",
    "properties": {
      "lock": {
        "type": "boolean",
        "description": "True to lock (disable sending), False to unlock (enable sending)"
      },
      "channel_id": {
        "type": "string",
        "description": "Optional: The Snowflake ID of the channel to lock/unlock"
      }
    },
    "required": ["lock"]
  }
}
