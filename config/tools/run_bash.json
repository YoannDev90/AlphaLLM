{
  "name": "run_bash",
  "description": "Execute bash/shell commands in a sandboxed environment using microsandbox",
  "inputSchema": {
    "type": "object",
    "properties": {
      "code": {
        "type": "string",
        "description": "Bash command or script to execute"
      },
      "timeout": {
        "type": "number",
        "description": "Execution timeout in seconds (default: 10)",
        "default": 10
      }
    },
    "required": ["code"]
  }
}
